Learnt about implementig abstraction were we defined methods like calculateMonthlySalary and did not implement it in the parent class and then implemented it in the child classes (HoulryEmployee and SalariedEmployee) and also made an interface (TaxableInterface) which acts as a contract but also lacks implementation in. The interface was also implemented in both the Employee type classes and the logic of the empty interface method was implemented.