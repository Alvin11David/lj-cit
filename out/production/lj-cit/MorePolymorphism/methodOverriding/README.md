Learnt about Method overriding as a type of polymorphism where by we use the @Override annotation which is used to make sure that the child class inherits either the method or the parameters from the parent class in that it checks if the method spelling in the child class matches that of the parent class such that actual inheritance is performed and if the method is incorrectky typed, it throws an error. And if its not used then the compiler will takeit ask a new method. 