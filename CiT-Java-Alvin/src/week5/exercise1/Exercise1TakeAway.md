I have learnt how to use polymorphism were an action can take many forms and for this case the send method/action in the parent class Notification took many forms in the child classes (EmailNotification, SmsNotification and PushNotification) and this happened through extending the parent class in each of the child classes, and we simply used the @Override annotation above the send method simply for the compiler to check if at all the method has been correctly typed as referenced in the parent class, and it catches any mistake by throwing an error.