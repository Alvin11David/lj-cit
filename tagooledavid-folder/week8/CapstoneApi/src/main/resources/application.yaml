spring:
  application:
    name: CapstoneApi

  datasource:
    url: jdbc:postgresql://localhost:5432/capstoneapidb
    username: postgres
    password: mugwadavid

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:

capstone-api:
  greeting: hello-from-CIT-Capstone-Api_project
  pagesize: 500